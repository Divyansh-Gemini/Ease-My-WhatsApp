<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.EaseMyWhatsApp" parent="Theme.Material3.DayNight">
        <item name="colorPrimary">@color/whatsapp_dark_fg</item>
        <item name="colorPrimaryVariant">@color/whatsapp_dark_fg</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSurface">@color/whatsapp_dark_fg</item>
        <item name="android:windowBackground">@color/whatsapp_dark_bg</item>

        <!-- status bar color -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
    </style>
</resources>