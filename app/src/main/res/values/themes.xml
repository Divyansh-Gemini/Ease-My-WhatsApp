<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.EaseMyWhatsApp" parent="Theme.Material3.DayNight">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/whatsapp_light_fg</item>
        <item name="colorPrimaryVariant">@color/whatsapp_light_fg</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- status bar color -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
    </style>

    <style name="Theme.EaseMyWhatsApp" parent="Base.Theme.EaseMyWhatsApp" />
</resources>